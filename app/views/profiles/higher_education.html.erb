<%= stylesheet_link_tag 'log', :media=> 'all' %>
<%= stylesheet_link_tag 'profile_about_me', :media => "all" %>
<%= javascript_include_tag 'cocoon' %>

<div class="tabbable tabs-left" style="margin-top: 25px"> <!-- Only required for left/right tabs -->
	<%= render 'layouts/profile'%>
	<div class="tab-content">
        <div class="tab-pane active" >
            <%= render 'about_me'%>
            <div id="my_information_green">	 
                <h1>Освіта</h1>
                <%= render 'education'%>
                <div id="secondary_education">
                    <h1>Вища освіта</h1>
                    <%= form_for @user, :url => {:controller=>"profiles", :action => "update_higher_education", :id=> current_user.id} do |f|  %>
                        <%= f.fields_for :higher_educations do |h_education| %>
                            <%= render 'higher_education_fields', :f=>h_education %>                                 
                        <%end%> 
                        <div class="links" style="width: 233px; margin-left: 180px">                 
                            <%= link_to_add_association "Додати Вищий Навчальний Заклад", f, :higher_educations, :partial =>"higher_education_fields"  %>       
                        </div>    
                        <%= f.submit "Зберегти"%>
                    <%end%>
                </div>
        	</div>            
        </div>        
	</div>
</div>