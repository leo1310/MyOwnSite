<%= stylesheet_link_tag 'log', :media=> 'all' %>
<%= stylesheet_link_tag 'profile_friends', :media=> 'all' %>
<%= javascript_include_tag 'friends/online' %>

<div class="tabbable tabs-left" style="margin-top: 25px"> <!-- Only required for left/right tabs -->
	<%= render 'layouts/profile'%>
	<div class="tab-content">
        <div class="tab-pane active" >
        	<%= render 'friends'%>
            <h1>Друзі Online</h1>
            <div class="friend_online">
            	<span id="count_friends_online"><span>
            </div>
            <table class="friend_all">
            	<% @friends.each do |friend|%>
            		<%@user_f = User.find(friend.friend)%>
            		<%unless @user_f.status_active.nil?%>
                          <%if (Time.now-@user_f.status_active) < 60%>
                                <%@active = true%>
                          <%else%>
                                <%@active = false%>
                          <%end%>
                    <%else%>
                          <%@active = false%>
                    <%end%>
            		<%if @user_f.count_time_logout < @user_f.sign_in_count and @active%>
	            		<thead>
	            			<td class="friend" style="width:140px">
	            				<a href="profiles/<%=friend.friend.to_s%>">          				
	            					<%= image_tag @user_f.avatar.url, :size => "128x128", :title=>@user_f.nik_name%>
	            				</a>
	            			</td>
	            			<td class="friends_all">
	            				<div class="friend_title">
	            					<%= @user_f.last_name%> <%= @user_f.name%>            				
	            				</div>	            				
								<div id="online">Online</div>								
								<% unless @user_f.higher_educations.blank? %>		
									<div class="title_high_school">
										<%= @user_f.higher_educations.first.high_school%>								
									</div>
								<%end%>
	            			</td>
	            			<td class="friend_options">
	            				<%= link_to "Написати повідомлення", "#"%></br>
	            				<%= link_to "Видалити з друзів", delete_friend_path(:id=>@user_f.id) %></br>
	            				<%= link_to "Переглянути друзів", "#"%></br>
	            				<%= link_to "Додати до списку", "#"%>            				
	            			</td>
	            			<%@online_users += 1 %>            			
	            		</thead>
	            		<tr >
	            			<td colspan="3">
	            				<div class="line">
			                	</div>	
			            	</td>
	            		</tr>
            		<%end%>            		
            	<%end%>
            	<input type="hidden" value=<%=@online_users %> id="online_users">
            </table>
        </div>        
	</div>
</div>